#include <stdio.h>
#include <ctype.h>

int main(void) {
    int i; // Variável de controle do loop

    for (i = 1; i < 3; i++) {
        printf("Aprendendo Linguagem C\n");
    }

    getch();
    return 0;
}
